version: "3.9"

services:
  eventos-db:
    container_name: eventos-db
    image: container-registry.oracle.com/database/express:21.3.0-xe
    environment:
      ORACLE_PWD: Netec_123
      ORACLE_SID: XE
      ORACLE_PDB: XEPDB1
      ORACLE_CHARACTERSET: AL32UTF8
    ports:
      - "1521:1521"
      - "5500:5500"
    volumes:
      - eventos-data:/opt/oracle/oradata
      - ./scripts/eventos:/opt/oracle/scripts/startup
    networks:
      - g6-pfinal-network
    healthcheck:
      test: ["CMD", "sh", "-c", "echo 'SELECT 1 FROM DUAL;' | sqlplus system/Netec_123@localhost:1521/XE"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  participantes-db:
    container_name: participantes-db
    image: container-registry.oracle.com/database/express:21.3.0-xe
    environment:
      ORACLE_PWD: Netec_123
      ORACLE_SID: XE
      ORACLE_PDB: XEPDB1
      ORACLE_CHARACTERSET: AL32UTF8
    ports:
      - "1522:1521"
      - "5501:5500"
    volumes:
      - participantes-data:/opt/oracle/oradata
      - ./scripts/participantes:/opt/oracle/scripts/startup
    networks:
      - g6-pfinal-network
    healthcheck:
      test: ["CMD", "sh", "-c", "echo 'SELECT 1 FROM DUAL;' | sqlplus system/Netec_123@localhost:1521/XE"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

  ms-eventos:
    image: ms-eventos:1.2
    container_name: ms-eventos
    ports:
      - "9084:9084"
    networks:
      - g6-pfinal-network
    depends_on:
      eventos-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9084/eventos"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 30s

  ms-participantes:
    image: ms-participantes:1.2
    container_name: ms-participantes
    ports:
      - "9081:9081"
    networks:
      - g6-pfinal-network
    depends_on:
      participantes-db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://127.0.0.1:9081/participantes"]
      interval: 30s
      timeout: 15s
      retries: 5
      start_period: 30s

volumes:
  eventos-data:
  participantes-data:

networks:
  g6-pfinal-network:
    external: true
    driver: bridge